#ifndef Exceptions
#define Exceptions
class QueueEmptyException{};                    //throw this exception if the queue is empty

// Throw Exception if Stack Is Full
class StackIsFullException
{};

// Throw Exception if item is not in stack
class ItemNotFoundException
{};

#endif
